# WhatsApp Bot на Node.js с Baileys

Stateful WhatsApp бот, созданный с использованием библиотеки `@whiskeysockets/baileys`. Поддерживает авторизацию через QR-код, сохранение сессии и отправку сообщений.

## 🚀 Возможности

- ✅ Авторизация через QR-код
- ✅ Сохранение и восстановление сессии между перезапусками
- ✅ Отправка текстовых сообщений по номеру телефона
- ✅ Проверка регистрации номера в WhatsApp
- ✅ Обработка входящих сообщений
- ✅ Автоматическое переподключение при разрыве соединения
- ✅ Graceful shutdown

## 📋 Требования

- Node.js >= 18.0.0
- npm или yarn
- Активный аккаунт WhatsApp

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/XasterismX/whatsapp-baileys-bot.git
cd whatsapp-baileys-bot
```

2. Установите зависимости:
```bash
npm install
```

## 🎯 Использование

### Запуск бота

```bash
npm start
```

При первом запуске появится QR-код в терминале:

1. Откройте WhatsApp на телефоне
2. Перейдите в **Настройки** → **Связанные устройства**
3. Нажмите **Связать устройство**
4. Отсканируйте QR-код из терминала

### Отправка сообщений

Редактируйте файл `index.js` и раскомментируйте пример:

```javascript
// В конце файла index.js раскомментируйте:
(async () => {
  const sock = await startWhatsAppBot();
  
  // Ждем подключения перед отправкой
  setTimeout(async () => {
    await sendMessage(sock, '79123456789', 'Привет! Это сообщение от бота.');
  }, 5000);
})();
```

**Формат номера:** без знака `+` и пробелов, например: `79123456789`

### Использование функции sendMessage

```javascript
import { sendMessage } from './index.js';

// sock - объект WhatsApp соединения
await sendMessage(sock, '79123456789', 'Ваше сообщение');
```

Функция возвращает объект:
```javascript
{
  success: true/false,
  messageId: 'id сообщения', // если успешно
  error: 'текст ошибки'      // если ошибка
}
```

## 📁 Структура проекта

```
whatsapp-baileys-bot/
├── index.js           # Основной файл приложения
├── package.json       # Зависимости и скрипты
├── .gitignore        # Исключения для git
├── README.md         # Документация
└── auth_info/        # Папка с данными сессии (создается автоматически)
```

## 🔐 Безопасность

⚠️ **Важно!** Папка `auth_info/` содержит приватные ключи вашей WhatsApp сессии. 

- Не добавляйте эту папку в Git (уже добавлена в `.gitignore`)
- Не делитесь этими файлами с другими
- При компрометации удалите папку и пересоздайте сессию

## 🛠 Технические детали

### Сохранение сессии

Сессия сохраняется автоматически в папку `auth_info/` с помощью `useMultiFileAuthState`. При перезапуске бот автоматически загружает сохраненные учетные данные.

### Обработка ошибок

Бот обрабатывает следующие сценарии:

- ✅ Номер не зарегистрирован в WhatsApp
- ✅ Потеря соединения (автоматическое переподключение)
- ✅ Выход из системы (требуется повторная авторизация)
- ✅ Таймауты соединения

### Входящие сообщения

Бот автоматически логирует все входящие сообщения. Раскомментируйте блок автоответа в `index.js` для реализации логики обработки:

```javascript
if (messageText.toLowerCase().includes('привет')) {
  await sock.sendMessage(from, { text: 'Привет! Чем могу помочь?' });
}
```

## 🐛 Устранение неполадок

### Проблема: QR-код не появляется
- Проверьте подключение к интернету
- Убедитесь, что используется Node.js >= 18.0.0

### Проблема: Сессия не сохраняется
- Проверьте права доступа к папке `auth_info/`
- Убедитесь, что папка не удаляется при перезапуске

### Проблема: Ошибка при отправке сообщения
- Проверьте формат номера (без `+` и пробелов)
- Убедитесь, что номер зарегистрирован в WhatsApp
- Дождитесь полного подключения перед отправкой

## 📚 Дополнительные ресурсы

- [Документация Baileys](https://github.com/WhiskeySockets/Baileys)
- [NPM пакет @whiskeysockets/baileys](https://www.npmjs.com/package/@whiskeysockets/baileys)

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются Pull Request'ы и Issues!

---

**Разработано для выполнения технического задания Backend-разработчика**
